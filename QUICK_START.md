# 快速部署指南

## 🚀 5 分钟部署到 Render

### 步骤 1: 推送代码到 GitHub

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Barber Shop App"

# 在 GitHub 创建仓库后，添加远程地址
git remote add origin https://github.com/YOUR_USERNAME/barberShop.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步骤 2: 在 Render 创建 Blueprint

1. 访问 [Render Dashboard](https://dashboard.render.com/)
2. 点击 **New +** → 选择 **Blueprint**
3. 连接您的 GitHub 账号（如果还没连接）
4. 选择 `barberShop` 仓库
5. Render 会自动检测到 `render.yaml` 文件
6. 点击 **Apply**

### 步骤 3: 等待部署完成

Render 将自动：
- ✅ 创建 PostgreSQL 数据库
- ✅ 部署后端 API 服务
- ✅ 部署前端应用
- ✅ 运行数据库迁移
- ✅ 生成测试数据

**等待时间**: 约 5-10 分钟

### 步骤 4: 验证部署

部署完成后，您将看到：

**后端服务**: `https://barber-shop-backend.onrender.com`
- 访问 `/api/v1/health` 检查健康状态

**前端应用**: `https://barber-shop-frontend.onrender.com`
- 直接访问查看应用

**数据库**: `barber-shop-db`
- 在 Render Dashboard 查看连接信息

### 步骤 5: 开始使用

1. 访问前端 URL
2. 使用测试账号登录：
   - 手机号: `13800138000`
   - 验证码: 任意（生产环境需配置真实短信服务）

---

## 🎯 自动部署

之后的每次代码更新：

```bash
git add .
git commit -m "Update: your changes"
git push
```

Render 将自动：
1. 检测到代码变更
2. 重新构建
3. 自动部署

---

## 🔧 常用操作

### 查看日志
1. 进入服务详情页
2. 点击 **Logs** 标签
3. 实时查看运行日志

### 重新部署
1. 进入服务详情页
2. 点击 **Manual Deploy**
3. 选择 **Deploy latest commit**

### 修改环境变量
1. 进入服务详情页
2. 点击 **Environment** 标签
3. 修改变量值
4. 点击 **Save Changes**
5. 服务会自动重启

### 运行数据库迁移
```bash
# 在后端服务的 Shell 中运行
npx prisma migrate deploy
```

---

## 📚 更多帮助

详细文档请查看：
- [完整部署教程](./DEPLOY.md)
- [项目说明](./README.md)
- [Render 官方文档](https://render.com/docs)

---

## ⚠️ 注意事项

### Free Plan 限制
- 15 分钟不活动后服务休眠
- 唤醒需要 30-60 秒
- 数据库存储限制 1GB
- 每月 750 小时免费时长

### 建议
- 生产环境建议升级到 Starter Plan ($7/月)
- 使用自定义域名提升专业度
- 配置真实的短信验证服务

---

**祝您部署顺利！🎉**
